<h2 id="mailing-list-header">Put me on the mailing list.</h2>

<%= form_for @user, html: {class: "form-group"}, remote: true do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :email, placeholder: "Enter Email Here...", class: "form-control", id: "nws-email" %>
  </div>
  <div class="actions">
    <%= f.submit "Request", class: "btn btn-primary" %>
  </div>
<% end %>

